<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Allowed Supplies</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<style>
  body {
  min-height: 100vh;
  background: linear-gradient(
    to bottom,
    #618286,
    #54ACBF,
    #26658C,
    #023859,
    #011C40
  );
}

</style>
<body >

<%- include("partials/navbar") %>

<div class="container mt-4">

  <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap">
    <h3 class="fw-bold mb-2">Allowed Supplies</h3>

    <a href="/ration" class="btn btn-outline-secondary btn-sm">
      ⬅ Back to Users
    </a>
  </div>

  <div class="card shadow-sm mb-4">
    <div class="card-body">
      <div class="row">
        <div class="col-md-4 mb-2">
          <strong>Name:</strong> <%= user.name %>
        </div>

        <div class="col-md-4 mb-2">
          <strong>Card Type:</strong>
          <span class="badge <%= user.cardType === 'BPL' ? 'bg-danger' : 'bg-success' %>">
            <%= user.cardType %>
          </span>
        </div>

        <div class="col-md-4 mb-2">
          <strong>Family Members:</strong> <%= user.members %>
        </div>
      </div>
    </div>
  </div>

  <div class="card shadow-sm">
    <div class="card-header fw-semibold">
      Monthly Entitlements (Calculated)
    </div>

    <ul class="list-group list-group-flush">

      <% if (supplies.length === 0) { %>
        <li class="list-group-item text-muted text-center">
          No supplies configured
        </li>
      <% } %>

      <% supplies.forEach(item => { 
           const allowedQty = item.perPersonQty * user.members
      %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <div>
            <strong><%= item.itemName %></strong>
            <div class="text-muted small">
              <%= item.perPersonQty %> kg × <%= user.members %> members
            </div>
          </div>

          <span class="badge bg-primary rounded-pill">
            <%= allowedQty %> kg
          </span>
        </li>
      <% }) %>

    </ul>
  </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
